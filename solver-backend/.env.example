# Browser/Vercel origin(s), comma-separated.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# auto = CUDA when available, CPU otherwise
# gpu = require CUDA
# cpu = force CPU
SOLVER_MODE=auto
